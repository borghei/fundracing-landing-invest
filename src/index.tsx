import React from 'react';
import ReactDOM from 'react-dom/client';
import FundRacing from './fund-racing';

const root = ReactDOM.createRoot(
  document.getElementById('fund-racing') as HTMLElement
);
root.render(
    <FundRacing />
);